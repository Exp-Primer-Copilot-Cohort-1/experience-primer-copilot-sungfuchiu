function skilsMember() {
    var member = document.getElementById("member");
    var memberName = member.options[member.selectedIndex].text;
    var memberValue = member.options[member.selectedIndex].value;
    var memberSkills = document.getElementById("memberSkills");
    var memberSkillsName = memberSkills.options[memberSkills.selectedIndex].text;
    var memberSkillsValue = memberSkills.options[memberSkills.selectedIndex].value;
    var memberSkillsList = document.getElementById("memberSkillsList");
    var memberSkillsListName = memberSkillsList.options[memberSkillsList.selectedIndex].text;
    var memberSkillsListValue = memberSkillsList.options[memberSkillsList.selectedIndex].value;
    var memberSkillsListValueName = memberSkillsList.options[memberSkillsList.selectedIndex].value;
    var memberSkillsListValueNameText = memberSkillsList.options[memberSkillsList.selectedIndex].text;
    var memberSkillsListValueNameTextLength = memberSkillsList.options[memberSkillsList.selectedIndex].text.length;
    var memberSkillsListValueNameTextLength = memberSkillsList.options[memberSkillsList.selectedIndex].text.length;
    var memberSkillsListValueNameTextLength = memberSkillsList.options[memberSkillsList.selectedIndex].text.length;
    var memberSkillsListValueNameTextLength = memberSkillsList.options[memberSkillsList.selectedIndex].text.length;
    var memberSkillsListValueNameTextLength = memberSkillsList.options[memberSkillsList.selectedIndex].text.length;
    var memberSkillsListValueNameTextLength = memberSkillsList.options[memberSkillsList.selectedIndex].text.length;
    var memberSkillsListValueNameTextLength = memberSkillsList.options[memberSkillsList.selectedIndex].text.length;
    var memberSkillsListValueNameTextLength = memberSkillsList.options[memberSkillsList.selectedIndex].text.length;
    var memberSkillsListValueNameTextLength = memberSkillsList.options[memberSkillsList.selectedIndex].text.length;
    var memberSkillsListValueNameTextLength = memberSkillsList.options[memberSkillsList.selectedIndex].text.length;

    var memberSkillsListValueNameTextLength = memberSkillsList.options[memberSkillsList.selectedIndex].text.length;
    var memberSkillsListValueNameTextLength = memberSkillsList.options[memberSkillsList.selectedIndex].text.length;

    var memberSkillsListValueNameTextLength = memberSkillsList.options[memberSkillsList.selectedIndex].text.length;
    var memberSkillsListValueNameTextLength = memberSkillsList.options[memberSkillsList.selectedIndex].text.length;

    var memberSkillsList = document.getElementById("memberSkillsList");
}